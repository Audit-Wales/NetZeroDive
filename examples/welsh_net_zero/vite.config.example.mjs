import { defineConfig } from 'vite'; import { fileURLToPath } from 'url'; import path from 'path'; const __dirname = path.dirname(fileURLToPath(import.meta.url)); export default defineConfig({ root: __dirname, resolve: { alias: { '/src': path.resolve(__dirname, '../../src') } }, build: { outDir: '../../dist-example-welsh', emptyOutDir: true, rollupOptions: { input: { main: path.resolve(__dirname, 'index.html'), tools1: path.resolve(__dirname, 'tools/coverage-growth.html'), tools2: path.resolve(__dirname, 'tools/sector-donut.html'), tools3: path.resolve(__dirname, 'tools/supply-chain-blocks.html') } } } });
